============================================================
STAGE 10.4 — CANONICAL JOB STORE / WORKER OWNERSHIP
============================================================

Purpose:
- identify the persisted orchestration job contract
- identify the job-store write API
- identify the service-level create-job API
- identify the live worker consumer
- determine how one canonical Universal Job ID can be preserved
- determine whether a Stage 10 adapter is required


============================================================
FILE: backend\server\orchestration\models.py
EXISTS: True
============================================================
LINE 25: def utc_now() -> datetime:
LINE 30: class OrchestrationJob:
LINE 63: class JobStep:
LINE 78: class JobStatusEvent:
LINE 88: class JobWorkerAssignment:
LINE 97: class JobResult:
LINE 106: class QueueStats:
LINE 115: class WorkerHeartbeat:

============================================================
FILE: backend\server\orchestration\job_store.py
EXISTS: True
============================================================
LINE 24: def _ensure_dir() -> None:
LINE 28: def _json_default(value: Any) -> Any:
LINE 34: def _read_json(path: Path, default: Any) -> Any:
LINE 44: def _write_json(path: Path, data: Any) -> None:
LINE 52: def _parse_datetime(value: Any) -> Optional[datetime]:
LINE 63: def _job_from_dict(data: Dict[str, Any]) -> OrchestrationJob:
LINE 97: def load_jobs() -> Dict[str, OrchestrationJob]:
LINE 108: def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
LINE 112: def get_job(job_id: str) -> Optional[OrchestrationJob]:
LINE 116: def create_job(
LINE 139: def update_job_status(
LINE 168: def update_job_progress(
LINE 237: def append_job_event(
LINE 261: def list_job_events(job_id: str) -> List[Dict[str, Any]]:

============================================================
FILE: backend\server\orchestration\service.py
EXISTS: True
============================================================
LINE 26: def create_orchestration_job(
LINE 55: def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
LINE 66: def list_orchestration_jobs() -> Dict[str, Any]:
LINE 74: def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
LINE 102: def mark_job_completed(
LINE 113: def mark_job_failed(
LINE 126: def cancel_job(
LINE 133: def update_orchestration_progress(
LINE 169: def service_health() -> Dict[str, Any]:

============================================================
FILE: backend\server\orchestration\queue.py
EXISTS: True
============================================================
LINE 62: def _flock(fh) -> None:
LINE 65: def _funlock(fh) -> None:
LINE 71: def _flock(fh) -> None:
LINE 74: def _funlock(fh) -> None:
LINE 80: def _dequeue_lock() -> Iterator[None]:
LINE 93: def _sort_key(job: OrchestrationJob):
LINE 97: def _queued_jobs(jobs: Optional[Dict[str, OrchestrationJob]] = None) -> List[OrchestrationJob]:
LINE 106: def enqueue_job(job_id: str) -> bool:
LINE 118: def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
LINE 148: def queue_length() -> int:
LINE 152: def refresh_queue_from_store() -> None:
LINE 161: def queue_snapshot() -> Dict[str, object]:
LINE 184: def is_job_queued(job_id: str) -> bool:

============================================================
2. JOB CREATION / STORE WRITE CONTRACT
============================================================

FILE: backend\server\orchestration\models.py
PATTERN: class OrchestrationJob
LINE 30
}


def utc_now() -> datetime:
    return datetime.now(timezone.utc)


@dataclass
>>> class OrchestrationJob:
    job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
    job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

FILE: backend\server\orchestration\models.py
PATTERN: job_id
LINE 31


def utc_now() -> datetime:
    return datetime.now(timezone.utc)


@dataclass
class OrchestrationJob:
>>>     job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
    job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)


FILE: backend\server\orchestration\models.py
PATTERN: job_id
LINE 65
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
>>>     job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str
    job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
    metadata: Dict[str, Any] = field(default_factory=dict)


@dataclass
class JobWorkerAssignment:
    assignment_id: str
    job_id: str
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
    job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0

FILE: backend\server\orchestration\models.py
PATTERN: job_id
LINE 80
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str
>>>     job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
    metadata: Dict[str, Any] = field(default_factory=dict)


@dataclass
class JobWorkerAssignment:
    assignment_id: str
    job_id: str
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
    job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
    metadata: Dict[str, Any] = field(default_factory=dict)




FILE: backend\server\orchestration\models.py
PATTERN: job_id
LINE 90
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
    metadata: Dict[str, Any] = field(default_factory=dict)


@dataclass
class JobWorkerAssignment:
    assignment_id: str
>>>     job_id: str
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
    job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
    metadata: Dict[str, Any] = field(default_factory=dict)




FILE: backend\server\orchestration\models.py
PATTERN: job_id
LINE 99
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
>>>     job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
    metadata: Dict[str, Any] = field(default_factory=dict)




FILE: backend\server\orchestration\models.py
PATTERN: payload
LINE 38
class OrchestrationJob:
    job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
>>>     payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
    job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str
    job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)

FILE: backend\server\orchestration\models.py
PATTERN: metadata
LINE 39
    job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
>>>     metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
    job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str
    job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
    metadata: Dict[str, Any] = field(default_factory=dict)

FILE: backend\server\orchestration\models.py
PATTERN: metadata
LINE 75

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
>>>     metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str
    job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
    metadata: Dict[str, Any] = field(default_factory=dict)


@dataclass
class JobWorkerAssignment:
    assignment_id: str
    job_id: str
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
    job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
    metadata: Dict[str, Any] = field(default_factory=dict)


FILE: backend\server\orchestration\models.py
PATTERN: metadata
LINE 84

@dataclass
class JobStatusEvent:
    event_id: str
    job_id: str
    old_status: str
    new_status: str
    created_at: datetime = field(default_factory=utc_now)
>>>     metadata: Dict[str, Any] = field(default_factory=dict)


@dataclass
class JobWorkerAssignment:
    assignment_id: str
    job_id: str
    worker_id: str
    assigned_at: datetime = field(default_factory=utc_now)
    released_at: Optional[datetime] = None


@dataclass
class JobResult:
    result_id: str
    job_id: str
    success: bool = True
    output: Dict[str, Any] = field(default_factory=dict)
    created_at: datetime = field(default_factory=utc_now)


@dataclass
class QueueStats:
    total_jobs: int = 0
    queued_jobs: int = 0
    running_jobs: int = 0
    completed_jobs: int = 0
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
    metadata: Dict[str, Any] = field(default_factory=dict)




FILE: backend\server\orchestration\models.py
PATTERN: metadata
LINE 119
    failed_jobs: int = 0


@dataclass
class WorkerHeartbeat:
    worker_id: str
    last_seen_at: datetime = field(default_factory=utc_now)
    status: str = "online"
>>>     metadata: Dict[str, Any] = field(default_factory=dict)




FILE: backend\server\orchestration\models.py
PATTERN: JOB_STATUS_QUEUED
LINE 9
from __future__ import annotations

from dataclasses import dataclass, field
from datetime import datetime, timezone
from typing import Any, Dict, Optional


JOB_STATUS_PENDING = "pending"
>>> JOB_STATUS_QUEUED = "queued"
JOB_STATUS_RUNNING = "running"
JOB_STATUS_COMPLETED = "completed"
JOB_STATUS_FAILED = "failed"
JOB_STATUS_CANCELLED = "cancelled"

VALID_JOB_STATUSES = {
    JOB_STATUS_PENDING,
    JOB_STATUS_QUEUED,
    JOB_STATUS_RUNNING,
    JOB_STATUS_COMPLETED,
    JOB_STATUS_FAILED,
    JOB_STATUS_CANCELLED,
}


def utc_now() -> datetime:
    return datetime.now(timezone.utc)


@dataclass
class OrchestrationJob:
    job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

FILE: backend\server\orchestration\models.py
PATTERN: JOB_STATUS_QUEUED
LINE 17
JOB_STATUS_QUEUED = "queued"
JOB_STATUS_RUNNING = "running"
JOB_STATUS_COMPLETED = "completed"
JOB_STATUS_FAILED = "failed"
JOB_STATUS_CANCELLED = "cancelled"

VALID_JOB_STATUSES = {
    JOB_STATUS_PENDING,
>>>     JOB_STATUS_QUEUED,
    JOB_STATUS_RUNNING,
    JOB_STATUS_COMPLETED,
    JOB_STATUS_FAILED,
    JOB_STATUS_CANCELLED,
}


def utc_now() -> datetime:
    return datetime.now(timezone.utc)


@dataclass
class OrchestrationJob:
    job_id: str
    workspace_id: str
    job_type: str
    status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass

FILE: backend\server\orchestration\models.py
PATTERN: JOB_STATUS_QUEUED
LINE 34
    return datetime.now(timezone.utc)


@dataclass
class OrchestrationJob:
    job_id: str
    workspace_id: str
    job_type: str
>>>     status: str = JOB_STATUS_QUEUED
    priority: int = 5
    created_at: datetime = field(default_factory=utc_now)
    updated_at: datetime = field(default_factory=utc_now)
    payload: Dict[str, Any] = field(default_factory=dict)
    metadata: Dict[str, Any] = field(default_factory=dict)
    assigned_worker_id: Optional[str] = None
    progress_percent: float = 0.0
    current_step: str = ""
    total_steps: int = 0
    processed_count: int = 0
    failed_count: int = 0
    warning_count: int = 0

    total_documents: int = 0
    processed_documents: int = 0
    failed_documents: int = 0
    skipped_documents: int = 0

    current_document_id: str = ""
    current_document_name: str = ""

    progress_message: str = ""
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    error_message: Optional[str] = None


@dataclass
class JobStep:
    step_id: str
    job_id: str
    step_name: str

    status: str = JOB_STATUS_PENDING
    progress_percent: float = 0.0

    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    message: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)

@dataclass
class JobStatusEvent:
    event_id: str

FILE: backend\server\orchestration\job_store.py
PATTERN: def create_job
LINE 116
def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


>>> def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)


FILE: backend\server\orchestration\job_store.py
PATTERN: def save_job
LINE 108
        return {}
    return {
        job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


>>> def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status

FILE: backend\server\orchestration\job_store.py
PATTERN: def update_job_status
LINE 139
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


>>> def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 65
    try:
        return datetime.fromisoformat(str(value))
    except Exception:
        return None


def _job_from_dict(data: Dict[str, Any]) -> OrchestrationJob:
    return OrchestrationJob(
>>>         job_id=str(data.get("job_id", "")),
        workspace_id=str(data.get("workspace_id", "")),
        job_type=str(data.get("job_type", "")),
        status=str(data.get("status", "queued")),
        priority=int(data.get("priority", 5)),
        created_at=_parse_datetime(data.get("created_at")) or utc_now(),
        updated_at=_parse_datetime(data.get("updated_at")) or utc_now(),
        payload=data.get("payload") or {},
        metadata=data.get("metadata") or {},
        assigned_worker_id=data.get("assigned_worker_id"),
        progress_percent=float(data.get("progress_percent", 0.0)),
        current_step=str(data.get("current_step", "")),
        total_steps=int(data.get("total_steps", 0)),
        processed_count=int(data.get("processed_count", 0)),
        failed_count=int(data.get("failed_count", 0)),
        warning_count=int(data.get("warning_count", 0)),

        total_documents=int(data.get("total_documents", 0)),
        processed_documents=int(data.get("processed_documents", 0)),
        failed_documents=int(data.get("failed_documents", 0)),
        skipped_documents=int(data.get("skipped_documents", 0)),

        current_document_id=str(data.get("current_document_id", "")),
        current_document_name=str(data.get("current_document_name", "")),

        progress_message=str(data.get("progress_message", "")),
        started_at=_parse_datetime(data.get("started_at")),
        completed_at=_parse_datetime(data.get("completed_at")),
        error_message=data.get("error_message"),
    )


def load_jobs() -> Dict[str, OrchestrationJob]:
    raw = _read_json(JOBS_FILE, {})
    if not isinstance(raw, dict):
        return {}
    return {
        job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 102
    )


def load_jobs() -> Dict[str, OrchestrationJob]:
    raw = _read_json(JOBS_FILE, {})
    if not isinstance(raw, dict):
        return {}
    return {
>>>         job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")


FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 103


def load_jobs() -> Dict[str, OrchestrationJob]:
    raw = _read_json(JOBS_FILE, {})
    if not isinstance(raw, dict):
        return {}
    return {
        job_id: _job_from_dict(item)
>>>         for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 109
    return {
        job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
>>>     _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 112
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


>>> def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 113
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
>>>     return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 124
def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
>>>     job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 126
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
>>>         job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 133
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
>>>     jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 135
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
>>>     append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 140
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
>>>     job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 149
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
>>>     if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 150
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
>>>         raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)


FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 152
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

>>>     job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 162
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

>>>     jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))


FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 164

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
>>>     append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 169

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
>>>     job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

    if total_documents is not None:
        job.total_documents = max(0, int(total_documents))

    if processed_documents is not None:

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 186
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
>>>     if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

    if total_documents is not None:
        job.total_documents = max(0, int(total_documents))

    if processed_documents is not None:
        job.processed_documents = max(0, int(processed_documents))

    if failed_documents is not None:
        job.failed_documents = max(0, int(failed_documents))

    if skipped_documents is not None:
        job.skipped_documents = max(0, int(skipped_documents))

    if current_document_id is not None:
        job.current_document_id = str(current_document_id)

    if current_document_name is not None:
        job.current_document_name = str(current_document_name)

    if metadata:
        job.metadata.update(metadata)


FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 187
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
>>>         raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

    if total_documents is not None:
        job.total_documents = max(0, int(total_documents))

    if processed_documents is not None:
        job.processed_documents = max(0, int(processed_documents))

    if failed_documents is not None:
        job.failed_documents = max(0, int(failed_documents))

    if skipped_documents is not None:
        job.skipped_documents = max(0, int(skipped_documents))

    if current_document_id is not None:
        job.current_document_id = str(current_document_id)

    if current_document_name is not None:
        job.current_document_name = str(current_document_name)

    if metadata:
        job.metadata.update(metadata)

    jobs[job_id] = job

FILE: backend\server\orchestration\job_store.py
PATTERN: job_id
LINE 189
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

>>>     job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

    if total_documents is not None:
        job.total_documents = max(0, int(total_documents))

    if processed_documents is not None:
        job.processed_documents = max(0, int(processed_documents))

    if failed_documents is not None:
        job.failed_documents = max(0, int(failed_documents))

    if skipped_documents is not None:
        job.skipped_documents = max(0, int(skipped_documents))

    if current_document_id is not None:
        job.current_document_id = str(current_document_id)

    if current_document_name is not None:
        job.current_document_name = str(current_document_name)

    if metadata:
        job.metadata.update(metadata)

    jobs[job_id] = job
    save_jobs(jobs)
    return job

FILE: backend\server\orchestration\job_store.py
PATTERN: payload
LINE 72
    return OrchestrationJob(
        job_id=str(data.get("job_id", "")),
        workspace_id=str(data.get("workspace_id", "")),
        job_type=str(data.get("job_type", "")),
        status=str(data.get("status", "queued")),
        priority=int(data.get("priority", 5)),
        created_at=_parse_datetime(data.get("created_at")) or utc_now(),
        updated_at=_parse_datetime(data.get("updated_at")) or utc_now(),
>>>         payload=data.get("payload") or {},
        metadata=data.get("metadata") or {},
        assigned_worker_id=data.get("assigned_worker_id"),
        progress_percent=float(data.get("progress_percent", 0.0)),
        current_step=str(data.get("current_step", "")),
        total_steps=int(data.get("total_steps", 0)),
        processed_count=int(data.get("processed_count", 0)),
        failed_count=int(data.get("failed_count", 0)),
        warning_count=int(data.get("warning_count", 0)),

        total_documents=int(data.get("total_documents", 0)),
        processed_documents=int(data.get("processed_documents", 0)),
        failed_documents=int(data.get("failed_documents", 0)),
        skipped_documents=int(data.get("skipped_documents", 0)),

        current_document_id=str(data.get("current_document_id", "")),
        current_document_name=str(data.get("current_document_name", "")),

        progress_message=str(data.get("progress_message", "")),
        started_at=_parse_datetime(data.get("started_at")),
        completed_at=_parse_datetime(data.get("completed_at")),
        error_message=data.get("error_message"),
    )


def load_jobs() -> Dict[str, OrchestrationJob]:
    raw = _read_json(JOBS_FILE, {})
    if not isinstance(raw, dict):
        return {}
    return {
        job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,

FILE: backend\server\orchestration\job_store.py
PATTERN: payload
LINE 119

def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
>>>     payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})

FILE: backend\server\orchestration\job_store.py
PATTERN: payload
LINE 129
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
>>>         payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 73
        job_id=str(data.get("job_id", "")),
        workspace_id=str(data.get("workspace_id", "")),
        job_type=str(data.get("job_type", "")),
        status=str(data.get("status", "queued")),
        priority=int(data.get("priority", 5)),
        created_at=_parse_datetime(data.get("created_at")) or utc_now(),
        updated_at=_parse_datetime(data.get("updated_at")) or utc_now(),
        payload=data.get("payload") or {},
>>>         metadata=data.get("metadata") or {},
        assigned_worker_id=data.get("assigned_worker_id"),
        progress_percent=float(data.get("progress_percent", 0.0)),
        current_step=str(data.get("current_step", "")),
        total_steps=int(data.get("total_steps", 0)),
        processed_count=int(data.get("processed_count", 0)),
        failed_count=int(data.get("failed_count", 0)),
        warning_count=int(data.get("warning_count", 0)),

        total_documents=int(data.get("total_documents", 0)),
        processed_documents=int(data.get("processed_documents", 0)),
        failed_documents=int(data.get("failed_documents", 0)),
        skipped_documents=int(data.get("skipped_documents", 0)),

        current_document_id=str(data.get("current_document_id", "")),
        current_document_name=str(data.get("current_document_name", "")),

        progress_message=str(data.get("progress_message", "")),
        started_at=_parse_datetime(data.get("started_at")),
        completed_at=_parse_datetime(data.get("completed_at")),
        error_message=data.get("error_message"),
    )


def load_jobs() -> Dict[str, OrchestrationJob]:
    raw = _read_json(JOBS_FILE, {})
    if not isinstance(raw, dict):
        return {}
    return {
        job_id: _job_from_dict(item)
        for job_id, item in raw.items()
        if isinstance(item, dict)
    }


def save_jobs(jobs: Dict[str, OrchestrationJob]) -> None:
    _write_json(JOBS_FILE, {job_id: asdict(job) for job_id, job in jobs.items()})


def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 120
def get_job(job_id: str) -> Optional[OrchestrationJob]:
    return load_jobs().get(job_id)


def create_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
>>>     metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 130
) -> OrchestrationJob:
    jobs = load_jobs()
    job_id = f"job_{uuid4().hex[:16]}"
    job = OrchestrationJob(
        job_id=job_id,
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
>>>         metadata=metadata or {},
        priority=priority,
    )
    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 142
    save_jobs(jobs)
    append_job_event(job_id, "none", job.status, {"reason": "job_created"})
    return job


def update_job_status(
    job_id: str,
    new_status: str,
>>>     metadata: Optional[Dict[str, Any]] = None,
    error_message: Optional[str] = None,
) -> OrchestrationJob:
    if new_status not in VALID_JOB_STATUSES:
        raise ValueError(f"Invalid job status: {new_status}")

    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    old_status = job.status
    job.status = new_status
    job.updated_at = utc_now()

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 164

    if error_message:
        job.error_message = error_message
        if new_status == JOB_STATUS_FAILED:
            job.progress_percent = min(job.progress_percent, 99.0)

    jobs[job_id] = job
    save_jobs(jobs)
>>>     append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 171
    save_jobs(jobs)
    append_job_event(job_id, old_status, new_status, metadata or {})
    return job


def update_job_progress(
    job_id: str,
    progress_percent: float,
>>>     metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    jobs = load_jobs()
    if job_id not in jobs:
        raise KeyError(f"Job not found: {job_id}")

    job = jobs[job_id]
    job.progress_percent = max(0.0, min(100.0, float(progress_percent)))
    job.updated_at = utc_now()

    if current_step is not None:
        job.current_step = str(current_step)

    if progress_message is not None:
        job.progress_message = str(progress_message)

    if total_steps is not None:
        job.total_steps = max(0, int(total_steps))

    if processed_count is not None:
        job.processed_count = max(0, int(processed_count))

    if failed_count is not None:
        job.failed_count = max(0, int(failed_count))

    if warning_count is not None:
        job.warning_count = max(0, int(warning_count))

    if total_documents is not None:
        job.total_documents = max(0, int(total_documents))

    if processed_documents is not None:
        job.processed_documents = max(0, int(processed_documents))


FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 229
        job.skipped_documents = max(0, int(skipped_documents))

    if current_document_id is not None:
        job.current_document_id = str(current_document_id)

    if current_document_name is not None:
        job.current_document_name = str(current_document_name)

>>>     if metadata:
        job.metadata.update(metadata)

    jobs[job_id] = job
    save_jobs(jobs)
    return job


def append_job_event(
    job_id: str,
    old_status: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> JobStatusEvent:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        events = {}

    event = JobStatusEvent(
        event_id=f"evt_{uuid4().hex[:16]}",
        job_id=job_id,
        old_status=old_status,
        new_status=new_status,
        metadata=metadata or {},
    )

    events.setdefault(job_id, [])
    events[job_id].append(asdict(event))
    _write_json(EVENTS_FILE, events)
    return event


def list_job_events(job_id: str) -> List[Dict[str, Any]]:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        return []
    return events.get(job_id, [])





FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 230

    if current_document_id is not None:
        job.current_document_id = str(current_document_id)

    if current_document_name is not None:
        job.current_document_name = str(current_document_name)

    if metadata:
>>>         job.metadata.update(metadata)

    jobs[job_id] = job
    save_jobs(jobs)
    return job


def append_job_event(
    job_id: str,
    old_status: str,
    new_status: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> JobStatusEvent:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        events = {}

    event = JobStatusEvent(
        event_id=f"evt_{uuid4().hex[:16]}",
        job_id=job_id,
        old_status=old_status,
        new_status=new_status,
        metadata=metadata or {},
    )

    events.setdefault(job_id, [])
    events[job_id].append(asdict(event))
    _write_json(EVENTS_FILE, events)
    return event


def list_job_events(job_id: str) -> List[Dict[str, Any]]:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        return []
    return events.get(job_id, [])





FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 241
    save_jobs(jobs)
    return job


def append_job_event(
    job_id: str,
    old_status: str,
    new_status: str,
>>>     metadata: Optional[Dict[str, Any]] = None,
) -> JobStatusEvent:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        events = {}

    event = JobStatusEvent(
        event_id=f"evt_{uuid4().hex[:16]}",
        job_id=job_id,
        old_status=old_status,
        new_status=new_status,
        metadata=metadata or {},
    )

    events.setdefault(job_id, [])
    events[job_id].append(asdict(event))
    _write_json(EVENTS_FILE, events)
    return event


def list_job_events(job_id: str) -> List[Dict[str, Any]]:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        return []
    return events.get(job_id, [])





FILE: backend\server\orchestration\job_store.py
PATTERN: metadata
LINE 252
    if not isinstance(events, dict):
        events = {}

    event = JobStatusEvent(
        event_id=f"evt_{uuid4().hex[:16]}",
        job_id=job_id,
        old_status=old_status,
        new_status=new_status,
>>>         metadata=metadata or {},
    )

    events.setdefault(job_id, [])
    events[job_id].append(asdict(event))
    _write_json(EVENTS_FILE, events)
    return event


def list_job_events(job_id: str) -> List[Dict[str, Any]]:
    events = _read_json(EVENTS_FILE, {})
    if not isinstance(events, dict):
        return []
    return events.get(job_id, [])





FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 50
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
>>>     if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job


FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 51
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
>>>         print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 55
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


>>> def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 56
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
>>>     job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)



FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 62

def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
>>>         "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 74
def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


>>> def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 84

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
>>>     job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 86
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
>>>         raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 92
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
>>>                 job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 99
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
>>>     return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 103

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
>>>     job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 109

def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
>>>     update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 110
def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
>>>     return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 114
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
>>>     job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 119


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
>>>         job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 127
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
>>>     job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 130
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
>>>     return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,

FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 134
def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
>>>     job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,
        JOB_STATUS_FAILED: 0,
        JOB_STATUS_CANCELLED: 0,
    }


FILE: backend\server\orchestration\service.py
PATTERN: job_id
LINE 151
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
>>>         job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,
        JOB_STATUS_FAILED: 0,
        JOB_STATUS_CANCELLED: 0,
    }

    for job in jobs.values():
        counts[job.status] = counts.get(job.status, 0) + 1

    return {
        "ok": True,
        "total_jobs": len(jobs),
        "status_counts": counts,
        "queue": queue_snapshot(),
    }

FILE: backend\server\orchestration\service.py
PATTERN: payload
LINE 29
    OrchestrationJob,
)
from .queue import enqueue_job, queue_snapshot


def create_orchestration_job(
    workspace_id: str,
    job_type: str,
>>>     payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    """Create and enqueue a job.

    NOTE on priority: jobs are served in ascending (priority, created_at)
    order — a LOWER number means served sooner. The default of 5 leaves
    room on both sides (e.g. 1 for urgent user-facing work, 9 for bulk
    background rebuilds).
    """
    job = create_job(
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:

FILE: backend\server\orchestration\service.py
PATTERN: payload
LINE 43
    NOTE on priority: jobs are served in ascending (priority, created_at)
    order — a LOWER number means served sooner. The default of 5 leaves
    room on both sides (e.g. 1 for urgent user-facing work, 9 for bulk
    background rebuilds).
    """
    job = create_job(
        workspace_id=workspace_id,
        job_type=job_type,
>>>         payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 30
)
from .queue import enqueue_job, queue_snapshot


def create_orchestration_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
>>>     metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    """Create and enqueue a job.

    NOTE on priority: jobs are served in ascending (priority, created_at)
    order — a LOWER number means served sooner. The default of 5 leaves
    room on both sides (e.g. 1 for urgent user-facing work, 9 for bulk
    background rebuilds).
    """
    job = create_job(
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 44
    order — a LOWER number means served sooner. The default of 5 leaves
    room on both sides (e.g. 1 for urgent user-facing work, 9 for bulk
    background rebuilds).
    """
    job = create_job(
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
>>>         metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


def list_orchestration_jobs() -> Dict[str, Any]:
    jobs = load_jobs()
    return {
        "jobs": [asdict(job) for job in jobs.values()],
        "queue": queue_snapshot(),
    }


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
    most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 80


def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:
    """Transition a job to RUNNING.

    FIX: made idempotent. dequeue_job() now claims jobs by flipping them to
    RUNNING atomically, so by the time the worker calls this the job is
    usually already running. Re-issuing the transition would be harmless in
>>>     most stores but is wasteful and can clobber claim metadata; if the job is
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )



FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 89
    already RUNNING we just return it (merging in the worker_id if it wasn't
    recorded yet).
    """
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
>>>         if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 92
    job = get_job(job_id)
    if job is None:
        raise KeyError(f"unknown job: {job_id}")

    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
>>>                 job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 96
    if job.status == JOB_STATUS_RUNNING:
        if worker_id and (job.metadata or {}).get("worker_id") != worker_id:
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

>>>     metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 98
            # Record the worker identity without re-transitioning status.
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
>>>         metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 99
            return update_job_status(
                job_id, JOB_STATUS_RUNNING, metadata={"worker_id": worker_id}
            )
        return job

    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
>>>     return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 104
    metadata: Dict[str, Any] = {}
    if worker_id:
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
>>>     metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 106
        metadata["worker_id"] = worker_id
    return update_job_status(job_id, JOB_STATUS_RUNNING, metadata=metadata)


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
>>>     # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 108


def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
>>>     # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 110
def mark_job_completed(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    # FIX: metadata was previously passed to BOTH the progress update and the
    # status update, merging the same dict twice. Progress just records 100%;
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
>>>     return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 116
    # metadata rides on the terminal status change.
    update_job_progress(job_id, 100.0)
    return update_job_status(job_id, JOB_STATUS_COMPLETED, metadata=metadata or {})


def mark_job_failed(
    job_id: str,
    error_message: str,
>>>     metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 121
def mark_job_failed(
    job_id: str,
    error_message: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(
        job_id,
        JOB_STATUS_FAILED,
>>>         metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 128
        JOB_STATUS_FAILED,
        metadata=metadata or {},
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
>>>     metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 130
        error_message=error_message,
    )


def cancel_job(
    job_id: str,
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
>>>     return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
    metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 136
    metadata: Optional[Dict[str, Any]] = None,
) -> OrchestrationJob:
    return update_job_status(job_id, JOB_STATUS_CANCELLED, metadata=metadata or {})


def update_orchestration_progress(
    job_id: str,
    progress_percent: float,
>>>     metadata: Optional[Dict[str, Any]] = None,
    current_step: Optional[str] = None,
    progress_message: Optional[str] = None,
    total_steps: Optional[int] = None,
    processed_count: Optional[int] = None,
    failed_count: Optional[int] = None,
    warning_count: Optional[int] = None,
    total_documents: Optional[int] = None,
    processed_documents: Optional[int] = None,
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
        metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,
        JOB_STATUS_FAILED: 0,
        JOB_STATUS_CANCELLED: 0,
    }

    for job in jobs.values():
        counts[job.status] = counts.get(job.status, 0) + 1

FILE: backend\server\orchestration\service.py
PATTERN: metadata
LINE 153
    failed_documents: Optional[int] = None,
    skipped_documents: Optional[int] = None,
    current_document_id: Optional[str] = None,
    current_document_name: Optional[str] = None,
) -> OrchestrationJob:
    return update_job_progress(
        job_id,
        progress_percent,
>>>         metadata=metadata or {},
        current_step=current_step,
        progress_message=progress_message,
        total_steps=total_steps,
        processed_count=processed_count,
        failed_count=failed_count,
        warning_count=warning_count,
        total_documents=total_documents,
        processed_documents=processed_documents,
        failed_documents=failed_documents,
        skipped_documents=skipped_documents,
        current_document_id=current_document_id,
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
        JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,
        JOB_STATUS_FAILED: 0,
        JOB_STATUS_CANCELLED: 0,
    }

    for job in jobs.values():
        counts[job.status] = counts.get(job.status, 0) + 1

    return {
        "ok": True,
        "total_jobs": len(jobs),
        "status_counts": counts,
        "queue": queue_snapshot(),
    }

FILE: backend\server\orchestration\service.py
PATTERN: JOB_STATUS_QUEUED
LINE 19
    load_jobs,
    update_job_progress,
    update_job_status,
)
from .models import (
    JOB_STATUS_CANCELLED,
    JOB_STATUS_COMPLETED,
    JOB_STATUS_FAILED,
>>>     JOB_STATUS_QUEUED,
    JOB_STATUS_RUNNING,
    OrchestrationJob,
)
from .queue import enqueue_job, queue_snapshot


def create_orchestration_job(
    workspace_id: str,
    job_type: str,
    payload: Optional[Dict[str, Any]] = None,
    metadata: Optional[Dict[str, Any]] = None,
    priority: int = 5,
) -> OrchestrationJob:
    """Create and enqueue a job.

    NOTE on priority: jobs are served in ascending (priority, created_at)
    order — a LOWER number means served sooner. The default of 5 leaves
    room on both sides (e.g. 1 for urgent user-facing work, 9 for bulk
    background rebuilds).
    """
    job = create_job(
        workspace_id=workspace_id,
        job_type=job_type,
        payload=payload or {},
        metadata=metadata or {},
        priority=priority,
    )
    # Enqueueing is implicit (queue membership == persisted "queued" status);
    # enqueue_job is retained as a sanity check that the job landed in the
    # store correctly.
    if not enqueue_job(job.job_id):
        print(f"[ORCHESTRATION_ENQUEUE_WARNING] job {job.job_id} not visible as queued after create")
    return job


def get_orchestration_job(job_id: str) -> Optional[Dict[str, Any]]:
    job = get_job(job_id)
    if job is None:
        return None

    return {
        "job": asdict(job),
        "events": list_job_events(job_id),
    }


FILE: backend\server\orchestration\service.py
PATTERN: JOB_STATUS_QUEUED
LINE 173
        current_document_name=current_document_name,
    )


def service_health() -> Dict[str, Any]:
    jobs = load_jobs()

    counts = {
>>>         JOB_STATUS_QUEUED: 0,
        JOB_STATUS_RUNNING: 0,
        JOB_STATUS_COMPLETED: 0,
        JOB_STATUS_FAILED: 0,
        JOB_STATUS_CANCELLED: 0,
    }

    for job in jobs.values():
        counts[job.status] = counts.get(job.status, 0) + 1

    return {
        "ok": True,
        "total_jobs": len(jobs),
        "status_counts": counts,
        "queue": queue_snapshot(),
    }

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 16
from typing import Dict, Iterator, List, Optional

from .job_store import get_job, load_jobs, update_job_status
from .models import JOB_STATUS_QUEUED, JOB_STATUS_RUNNING, OrchestrationJob

# ---------------------------------------------------------------------------
# Design notes (what changed and why)
#
>>> # The previous implementation kept a module-level list (_queue_job_ids) that
# was rebuilt from the job store on every call via refresh_queue_from_store().
# Because the store was always authoritative, the list carried no information
# of its own — but it created two real problems:
#
#   1. NON-ATOMIC DEQUEUE: dequeue_job() removed a job from the local list
#      while its status in the store remained "queued" until the worker later
#      called mark_job_running(). Any concurrent worker (thread OR separate
#      process) refreshing from the store in that window would re-discover the
#      job and dequeue it again -> duplicate processing.
#
#   2. STATE DRIFT: two sources of truth that had to be defended against each
#      other in multiple places (dead "clear()" branch, redundant existence
#      checks, 2-3 load_jobs() calls per operation).
#
# This version deletes the in-memory list entirely. The job store IS the
# queue: a job is "queued" iff its persisted status is JOB_STATUS_QUEUED.
# dequeue_job() now CLAIMS the job atomically — it transitions the job to
# JOB_STATUS_RUNNING inside a cross-process lock before returning it — so a
# job can be handed to exactly one worker. This also means the queue trivially
# survives process restarts: anything persisted as "queued" is picked up.
#
# Ordering is unchanged: ascending (priority, created_at) — i.e. LOWER
# priority number is served first, ties broken oldest-first (FIFO). job_id is
# appended as a final tie-breaker so ordering is fully deterministic.
# ---------------------------------------------------------------------------

# Cross-process lock file guarding the dequeue critical section
# (read store -> pick job -> flip status). Lives next to this module by
# default; override with ORCHESTRATION_QUEUE_LOCK_PATH if the package
# directory is read-only in your deployment.
_LOCK_PATH = Path(
    os.environ.get(
        "ORCHESTRATION_QUEUE_LOCK_PATH",
        str(Path(__file__).resolve().parent / ".queue.lock"),
    )
)

# In-process lock: cheaper than the file lock for threads within one process,
# and required anyway because fcntl/msvcrt locks are per-process, not
# per-thread.
_thread_lock = threading.Lock()

try:
    import fcntl  # POSIX


FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 39
# This version deletes the in-memory list entirely. The job store IS the
# queue: a job is "queued" iff its persisted status is JOB_STATUS_QUEUED.
# dequeue_job() now CLAIMS the job atomically — it transitions the job to
# JOB_STATUS_RUNNING inside a cross-process lock before returning it — so a
# job can be handed to exactly one worker. This also means the queue trivially
# survives process restarts: anything persisted as "queued" is picked up.
#
# Ordering is unchanged: ascending (priority, created_at) — i.e. LOWER
>>> # priority number is served first, ties broken oldest-first (FIFO). job_id is
# appended as a final tie-breaker so ordering is fully deterministic.
# ---------------------------------------------------------------------------

# Cross-process lock file guarding the dequeue critical section
# (read store -> pick job -> flip status). Lives next to this module by
# default; override with ORCHESTRATION_QUEUE_LOCK_PATH if the package
# directory is read-only in your deployment.
_LOCK_PATH = Path(
    os.environ.get(
        "ORCHESTRATION_QUEUE_LOCK_PATH",
        str(Path(__file__).resolve().parent / ".queue.lock"),
    )
)

# In-process lock: cheaper than the file lock for threads within one process,
# and required anyway because fcntl/msvcrt locks are per-process, not
# per-thread.
_thread_lock = threading.Lock()

try:
    import fcntl  # POSIX

    def _flock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_EX)

    def _funlock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_UN)

except ImportError:  # Windows
    import msvcrt

    def _flock(fh) -> None:
        msvcrt.locking(fh.fileno(), msvcrt.LK_LOCK, 1)

    def _funlock(fh) -> None:
        fh.seek(0)
        msvcrt.locking(fh.fileno(), msvcrt.LK_UNLCK, 1)


@contextmanager
def _dequeue_lock() -> Iterator[None]:
    """Serialize the select-and-claim critical section across threads and
    processes on this machine."""
    with _thread_lock:
        _LOCK_PATH.parent.mkdir(parents=True, exist_ok=True)

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 94
            _flock(fh)
            try:
                yield
            finally:
                _funlock(fh)


def _sort_key(job: OrchestrationJob):
>>>     return (job.priority, job.created_at, job.job_id)


def _queued_jobs(jobs: Optional[Dict[str, OrchestrationJob]] = None) -> List[OrchestrationJob]:
    """All jobs currently persisted as queued, in service order."""
    if jobs is None:
        jobs = load_jobs()
    queued = [job for job in jobs.values() if job.status == JOB_STATUS_QUEUED]
    queued.sort(key=_sort_key)
    return queued


def enqueue_job(job_id: str) -> bool:
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
    JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 106
    """All jobs currently persisted as queued, in service order."""
    if jobs is None:
        jobs = load_jobs()
    queued = [job for job in jobs.values() if job.status == JOB_STATUS_QUEUED]
    queued.sort(key=_sort_key)
    return queued


>>> def enqueue_job(job_id: str) -> bool:
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
    JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())



FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 114
def enqueue_job(job_id: str) -> bool:
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
    JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
>>>     job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 135
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
>>>                     candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
                "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")
                    else str(job.created_at)
                ),
            }
            for job in queued
        ],

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 166
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
>>>         "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
                "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")
                    else str(job.created_at)
                ),
            }
            for job in queued
        ],
    }


def is_job_queued(job_id: str) -> bool:
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 169
def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
>>>                 "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")
                    else str(job.created_at)
                ),
            }
            for job in queued
        ],
    }


def is_job_queued(job_id: str) -> bool:
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 184
                    else str(job.created_at)
                ),
            }
            for job in queued
        ],
    }


>>> def is_job_queued(job_id: str) -> bool:
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED

FILE: backend\server\orchestration\queue.py
PATTERN: job_id
LINE 185
                ),
            }
            for job in queued
        ],
    }


def is_job_queued(job_id: str) -> bool:
>>>     job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED

FILE: backend\server\orchestration\queue.py
PATTERN: metadata
LINE 122
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
>>>     (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [

FILE: backend\server\orchestration\queue.py
PATTERN: metadata
LINE 130
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
>>>             metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
                "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")

FILE: backend\server\orchestration\queue.py
PATTERN: metadata
LINE 132

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
>>>                 metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
                "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")
                    else str(job.created_at)
                ),

FILE: backend\server\orchestration\queue.py
PATTERN: metadata
LINE 137
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
>>>                     metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None


def queue_snapshot() -> Dict[str, object]:
    queued = _queued_jobs()

    return {
        "queue_length": len(queued),
        "queued_job_ids": [job.job_id for job in queued],
        "queued_jobs": [
            {
                "job_id": job.job_id,
                "workspace_id": job.workspace_id,
                "job_type": job.job_type,
                "priority": job.priority,
                "created_at": (
                    job.created_at.isoformat()
                    if hasattr(job.created_at, "isoformat")
                    else str(job.created_at)
                ),
            }
            for job in queued
        ],
    }


FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 11

import os
import threading
from contextlib import contextmanager
from pathlib import Path
from typing import Dict, Iterator, List, Optional

from .job_store import get_job, load_jobs, update_job_status
>>> from .models import JOB_STATUS_QUEUED, JOB_STATUS_RUNNING, OrchestrationJob

# ---------------------------------------------------------------------------
# Design notes (what changed and why)
#
# The previous implementation kept a module-level list (_queue_job_ids) that
# was rebuilt from the job store on every call via refresh_queue_from_store().
# Because the store was always authoritative, the list carried no information
# of its own — but it created two real problems:
#
#   1. NON-ATOMIC DEQUEUE: dequeue_job() removed a job from the local list
#      while its status in the store remained "queued" until the worker later
#      called mark_job_running(). Any concurrent worker (thread OR separate
#      process) refreshing from the store in that window would re-discover the
#      job and dequeue it again -> duplicate processing.
#
#   2. STATE DRIFT: two sources of truth that had to be defended against each
#      other in multiple places (dead "clear()" branch, redundant existence
#      checks, 2-3 load_jobs() calls per operation).
#
# This version deletes the in-memory list entirely. The job store IS the
# queue: a job is "queued" iff its persisted status is JOB_STATUS_QUEUED.
# dequeue_job() now CLAIMS the job atomically — it transitions the job to
# JOB_STATUS_RUNNING inside a cross-process lock before returning it — so a
# job can be handed to exactly one worker. This also means the queue trivially
# survives process restarts: anything persisted as "queued" is picked up.
#
# Ordering is unchanged: ascending (priority, created_at) — i.e. LOWER
# priority number is served first, ties broken oldest-first (FIFO). job_id is
# appended as a final tie-breaker so ordering is fully deterministic.
# ---------------------------------------------------------------------------

# Cross-process lock file guarding the dequeue critical section
# (read store -> pick job -> flip status). Lives next to this module by
# default; override with ORCHESTRATION_QUEUE_LOCK_PATH if the package
# directory is read-only in your deployment.
_LOCK_PATH = Path(
    os.environ.get(
        "ORCHESTRATION_QUEUE_LOCK_PATH",
        str(Path(__file__).resolve().parent / ".queue.lock"),
    )
)

# In-process lock: cheaper than the file lock for threads within one process,
# and required anyway because fcntl/msvcrt locks are per-process, not
# per-thread.

FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 32
#      process) refreshing from the store in that window would re-discover the
#      job and dequeue it again -> duplicate processing.
#
#   2. STATE DRIFT: two sources of truth that had to be defended against each
#      other in multiple places (dead "clear()" branch, redundant existence
#      checks, 2-3 load_jobs() calls per operation).
#
# This version deletes the in-memory list entirely. The job store IS the
>>> # queue: a job is "queued" iff its persisted status is JOB_STATUS_QUEUED.
# dequeue_job() now CLAIMS the job atomically — it transitions the job to
# JOB_STATUS_RUNNING inside a cross-process lock before returning it — so a
# job can be handed to exactly one worker. This also means the queue trivially
# survives process restarts: anything persisted as "queued" is picked up.
#
# Ordering is unchanged: ascending (priority, created_at) — i.e. LOWER
# priority number is served first, ties broken oldest-first (FIFO). job_id is
# appended as a final tie-breaker so ordering is fully deterministic.
# ---------------------------------------------------------------------------

# Cross-process lock file guarding the dequeue critical section
# (read store -> pick job -> flip status). Lives next to this module by
# default; override with ORCHESTRATION_QUEUE_LOCK_PATH if the package
# directory is read-only in your deployment.
_LOCK_PATH = Path(
    os.environ.get(
        "ORCHESTRATION_QUEUE_LOCK_PATH",
        str(Path(__file__).resolve().parent / ".queue.lock"),
    )
)

# In-process lock: cheaper than the file lock for threads within one process,
# and required anyway because fcntl/msvcrt locks are per-process, not
# per-thread.
_thread_lock = threading.Lock()

try:
    import fcntl  # POSIX

    def _flock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_EX)

    def _funlock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_UN)

except ImportError:  # Windows
    import msvcrt

    def _flock(fh) -> None:
        msvcrt.locking(fh.fileno(), msvcrt.LK_LOCK, 1)

    def _funlock(fh) -> None:
        fh.seek(0)
        msvcrt.locking(fh.fileno(), msvcrt.LK_UNLCK, 1)


FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 101
def _sort_key(job: OrchestrationJob):
    return (job.priority, job.created_at, job.job_id)


def _queued_jobs(jobs: Optional[Dict[str, OrchestrationJob]] = None) -> List[OrchestrationJob]:
    """All jobs currently persisted as queued, in service order."""
    if jobs is None:
        jobs = load_jobs()
>>>     queued = [job for job in jobs.values() if job.status == JOB_STATUS_QUEUED]
    queued.sort(key=_sort_key)
    return queued


def enqueue_job(job_id: str) -> bool:
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
    JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 110
    queued.sort(key=_sort_key)
    return queued


def enqueue_job(job_id: str) -> bool:
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
>>>     JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
    job = get_job(job_id)
    return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read

FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 115
    """Kept for API compatibility.

    Enqueueing is now implicit: create_job persists the job with status
    JOB_STATUS_QUEUED, and that persisted status is the queue membership.
    This function only verifies the job actually exists in a queued state,
    which catches caller bugs (e.g. enqueueing an ID that was never created).
    """
    job = get_job(job_id)
>>>     return job is not None and job.status == JOB_STATUS_QUEUED


def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
    """Atomically claim and return the highest-priority queued job.

    The returned job has ALREADY been transitioned to JOB_STATUS_RUNNING
    (with claim metadata) under a cross-process lock, so no other worker
    can receive the same job. Returns None when nothing is queued.

    Workers may still call mark_job_running() afterwards; with the
    corrected service.py that call is idempotent.
    """
    with _dequeue_lock():
        for candidate in _queued_jobs():
            metadata: Dict[str, object] = {"claimed_by_dequeue": True}
            if worker_id:
                metadata["worker_id"] = worker_id
            try:
                claimed = update_job_status(
                    candidate.job_id,
                    JOB_STATUS_RUNNING,
                    metadata=metadata,
                )
            except Exception:
                # Job vanished or store hiccuped between read and write —
                # skip it and try the next candidate rather than failing
                # the whole dequeue.
                continue
            return claimed
        return None


def queue_length() -> int:
    return len(_queued_jobs())


def refresh_queue_from_store() -> None:
    """Kept for API compatibility.

    There is no longer any in-memory queue state to refresh — every read
    goes straight to the job store — so this is a no-op.
    """
    return None



FILE: backend\server\orchestration\queue.py
PATTERN: JOB_STATUS_QUEUED
LINE 186
            }
            for job in queued
        ],
    }


def is_job_queued(job_id: str) -> bool:
    job = get_job(job_id)
>>>     return job is not None and job.status == JOB_STATUS_QUEUED

============================================================
3. ACTUAL DEQUEUE CALLERS / WORKERS
============================================================

PATTERN: dequeue_job(
MATCHES: 4
FILE: backend\server\orchestration\queue.py
LINE 21: #   1. NON-ATOMIC DEQUEUE: dequeue_job() removed a job from the local list
FILE: backend\server\orchestration\queue.py
LINE 33: # dequeue_job() now CLAIMS the job atomically — it transitions the job to
FILE: backend\server\orchestration\queue.py
LINE 118: def dequeue_job(worker_id: str | None = None) -> Optional[OrchestrationJob]:
FILE: backend\server\orchestration\service.py
LINE 77: FIX: made idempotent. dequeue_job() now claims jobs by flipping them to

PATTERN: dispatch_registered_runtime_handler(
MATCHES: 2
FILE: backend\server\runtime\universal_runtime_registration.py
LINE 945: def dispatch_registered_runtime_handler(
FILE: backend\server\runtime\universal_runtime_registration.py
LINE 1080: dispatch_registered_runtime_handler(

PATTERN: mark_job_running(
MATCHES: 3
FILE: backend\server\orchestration\queue.py
LINE 23: #      called mark_job_running(). Any concurrent worker (thread OR separate
FILE: backend\server\orchestration\queue.py
LINE 125: Workers may still call mark_job_running() afterwards; with the
FILE: backend\server\orchestration\service.py
LINE 74: def mark_job_running(job_id: str, worker_id: str | None = None) -> OrchestrationJob:

PATTERN: mark_job_complete(
MATCHES: 0

PATTERN: mark_job_failed(
MATCHES: 1
FILE: backend\server\orchestration\service.py
LINE 113: def mark_job_failed(

============================================================
4. UNIVERSAL JOB CONTRACT → ORCHESTRATION CONSUMERS
============================================================

PATTERN: UniversalJob
MATCHES: 147
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 118: class UniversalJobContractError(ValueError):
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 139: class UniversalJobStatus(str, enum.Enum):
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 158: def coerce(cls, value: Any) -> "UniversalJobStatus":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 166: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 172: class UniversalJobPriority(enum.IntEnum):
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 189: def coerce(cls, value: Any) -> "UniversalJobPriority":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 193: raise UniversalJobContractError("priority must not be a boolean")
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 205: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 222: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 239: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 246: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 476: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 486: class UniversalJobValidationReport:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 499: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 519: class UniversalJobProgress:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 585: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 601: def from_value(cls, value: Any) -> "UniversalJobProgress":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 609: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 610: "progress must be a mapping or UniversalJobProgress"
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 615: class UniversalJobCostRecord:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 654: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 669: def from_value(cls, value: Any) -> "UniversalJobCostRecord":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 677: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 678: "cost_record must be a mapping or UniversalJobCostRecord"
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 687: class UniversalJob:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 692: sequences are tuples. Construction raises ``UniversalJobContractError``
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 711: priority: UniversalJobPriority = UniversalJobPriority.NORMAL
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 712: status: UniversalJobStatus = UniversalJobStatus.CREATED
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 731: progress: UniversalJobProgress = field(default_factory=UniversalJobProgress)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 744: cost_record: UniversalJobCostRecord = field(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 745: default_factory=UniversalJobCostRecord
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 801: set_(self, "priority", UniversalJobPriority.coerce(self.priority))
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 802: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 804: set_(self, "priority", UniversalJobPriority.NORMAL)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 807: set_(self, "status", UniversalJobStatus.coerce(self.status))
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 808: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 810: set_(self, "status", UniversalJobStatus.CREATED)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 853: set_(self, "progress", UniversalJobProgress.from_value(self.progress))
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 854: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 856: set_(self, "progress", UniversalJobProgress())
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 877: set_(self, "cost_record", UniversalJobCostRecord.from_value(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 879: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 881: set_(self, "cost_record", UniversalJobCostRecord())
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 911: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 935: return self.lease_owner is not None and self.status == UniversalJobStatus.LEASED
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 939: def evolve(self, **changes: Any) -> "UniversalJob":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 947: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1037: ) -> "UniversalJob":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1042: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1052: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1070: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1088: UniversalJobPriority.coerce(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1095: UniversalJobStatus.coerce(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1102: UniversalJobProgress.from_value(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1109: UniversalJobCostRecord.from_value(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1119: def from_json(cls, text: str) -> "UniversalJob":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1123: raise UniversalJobContractError(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1128: def reconstruct(cls, data: Mapping[str, Any]) -> "UniversalJob":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1194: if not isinstance(other, UniversalJob):
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1200: def validate(self) -> UniversalJobValidationReport:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1212: UniversalJobStatus.SUCCEEDED,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1213: UniversalJobStatus.FAILED,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1214: UniversalJobStatus.CANCELLED,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1215: UniversalJobStatus.DEAD_LETTER,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1216: UniversalJobStatus.EXPIRED,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1221: f.name for f in fields(UniversalJob)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1229: def validate_universal_job(candidate: Any) -> UniversalJobValidationReport:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1232: A :class:`UniversalJob` instance is valid by construction. A mapping is
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1234: violations. Returns an immutable :class:`UniversalJobValidationReport`.
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1236: if isinstance(candidate, UniversalJob):
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1237: return UniversalJobValidationReport(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1245: return UniversalJobValidationReport(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1247: violations=("candidate must be a UniversalJob or mapping",),
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1257: UniversalJob.from_dict(candidate)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1258: except UniversalJobContractError as exc:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1260: return UniversalJobValidationReport(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1267: return UniversalJobValidationReport(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1280: return UniversalJobValidationReport(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1295: "UniversalJob",
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1296: "UniversalJobValidationReport",

PATTERN: to_canonical_dict
MATCHES: 138
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 327: def to_canonical_dict(self) -> dict[str, Any]:
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 371: def to_canonical_dict(self) -> dict[str, Any]:
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 377: "checks": [check.to_canonical_dict() for check in self.checks],
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 413: def to_canonical_dict(self) -> dict[str, Any]:
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 431: "sections": [s.to_canonical_dict() for s in self.sections],
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 478: text = _canonical_json(report.to_canonical_dict())
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 707: "sections": [s.to_canonical_dict() for s in sections],
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 276: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 279: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 485: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 488: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 570: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 576: record.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 982: record.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\audit.py
LINE 1008: record.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 225: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 293: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 306: check.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 429: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 459: section.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 788: .to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1008: .to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1010: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1664: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1672: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1719: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1732: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1755: .to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1851: ).to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 1867: ).to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2243: section.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2308: report.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 507: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 513: self.entry.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 614: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 656: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 661: "diff": self.diff.to_canonical_dict(),
FILE: backend\server\runtime\runtime_schema\change_detection.py
LINE 671: item.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\compatibility.py
LINE 138: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\compatibility.py
LINE 268: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\compatibility.py
LINE 294: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\compatibility.py
LINE 313: violation.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\definitions.py
LINE 742: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\definitions.py
LINE 914: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\deprecation.py
LINE 324: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\deprecation.py
LINE 382: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\deprecation.py
LINE 473: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\deprecation.py
LINE 476: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\diff_engine.py
LINE 137: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\diff_engine.py
LINE 245: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\diff_engine.py
LINE 268: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\diff_engine.py
LINE 292: entry.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\diff_engine.py
LINE 870: entry.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 98: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 206: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 220: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 231: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 249: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 273: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 296: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 323: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 343: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 367: ).to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\loader.py
LINE 404: result.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\migration.py
LINE 194: self.to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\migration.py
LINE 205: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\migration.py
LINE 391: self.to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\migration.py
LINE 419: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\migration.py
LINE 448: step.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\namespaces.py
LINE 133: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\namespaces.py
LINE 136: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\namespaces.py
LINE 435: record.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 204: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 207: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 336: self.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 339: def to_canonical_dict(
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 350: self.new_owner.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 803: subject: owner.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\ownership.py
LINE 808: record.to_canonical_dict()
FILE: backend\server\runtime\runtime_schema\registry.py
LINE 181: self.to_canonical_dict()

PATTERN: create_universal_job
MATCHES: 3
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 664: def create_universal_job(
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 813: "canonical_operation": "create_universal_job",
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 842: "create_universal_job",

PATTERN: payload_reference
MATCHES: 15
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 650: "payload_reference":
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 55: "payload_reference",
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 708: payload_reference: Optional[str] = None
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 797: set_(self, "payload_reference", _normalize_optional_text(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 798: self.payload_reference, name="payload_reference", violations=violations))
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 964: "payload_reference": self.payload_reference,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 464: payload_reference: Optional[str] = None
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 604: payload_reference = _first_text(
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 605: request.payload_reference,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 606: payload.get("payload_reference"),
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 647: payload_reference=payload_reference,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 674: payload_reference: Optional[str] = None,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 700: payload_reference=payload_reference,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 746: payload_reference=normalized.payload_reference,

PATTERN: idempotency_key
MATCHES: 18
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 72: "idempotency_key",
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 739: idempotency_key: Optional[str] = None
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 867: set_(self, "idempotency_key", _normalize_identifier(
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 868: self.idempotency_key, name="idempotency_key", violations=violations,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 981: "idempotency_key": self.idempotency_key,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1144: "idempotency_key": self.idempotency_key,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 470: idempotency_key: Optional[str] = None
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 653: idempotency_key=_clean_text(request.idempotency_key) or None,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 680: idempotency_key: Optional[str] = None,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 710: idempotency_key=idempotency_key,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 768: idempotency_key=normalized.idempotency_key,

PATTERN: contract_version
MATCHES: 317
FILE: backend\server\runtime\runtime_compatibility.py
LINE 723: manifest.service_contract_version
FILE: backend\server\runtime\runtime_compatibility.py
LINE 726: manifest.job_contract_version
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 47: FOUNDATION_CERTIFICATION_CONTRACT_VERSION: str = "1.0.0"
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 397: certification_contract_version: str
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 418: "certification_contract_version": (
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 419: self.certification_contract_version
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 607: "contract": FOUNDATION_CERTIFICATION_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 667: "contract": FOUNDATION_CERTIFICATION_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 681: "contract": FOUNDATION_CERTIFICATION_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 694: "certification_contract_version": (
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 695: FOUNDATION_CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 717: certification_contract_version=(
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 718: FOUNDATION_CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 1265: FOUNDATION_CERTIFICATION_CONTRACT_VERSION == "1.0.0",
FILE: backend\server\runtime\runtime_foundation_certification.py
LINE 1281: "FOUNDATION_CERTIFICATION_CONTRACT_VERSION",
FILE: backend\server\runtime\runtime_versioning.py
LINE 27: "DEFAULT_JOB_CONTRACT_VERSION",
FILE: backend\server\runtime\runtime_versioning.py
LINE 30: "DEFAULT_SERVICE_CONTRACT_VERSION",
FILE: backend\server\runtime\runtime_versioning.py
LINE 56: DEFAULT_SERVICE_CONTRACT_VERSION = "0.1.0"
FILE: backend\server\runtime\runtime_versioning.py
LINE 57: DEFAULT_JOB_CONTRACT_VERSION = "0.1.0"
FILE: backend\server\runtime\runtime_versioning.py
LINE 326: service_contract_version: RuntimeSemanticVersion
FILE: backend\server\runtime\runtime_versioning.py
LINE 327: job_contract_version: RuntimeSemanticVersion
FILE: backend\server\runtime\runtime_versioning.py
LINE 412: "service_contract_version": str(
FILE: backend\server\runtime\runtime_versioning.py
LINE 413: self.service_contract_version
FILE: backend\server\runtime\runtime_versioning.py
LINE 415: "job_contract_version": str(
FILE: backend\server\runtime\runtime_versioning.py
LINE 416: self.job_contract_version
FILE: backend\server\runtime\runtime_versioning.py
LINE 745: service_contract_version: (
FILE: backend\server\runtime\runtime_versioning.py
LINE 747: ) = DEFAULT_SERVICE_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_versioning.py
LINE 748: job_contract_version: (
FILE: backend\server\runtime\runtime_versioning.py
LINE 750: ) = DEFAULT_JOB_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_versioning.py
LINE 798: service_contract_version=(
FILE: backend\server\runtime\runtime_versioning.py
LINE 800: service_contract_version
FILE: backend\server\runtime\runtime_versioning.py
LINE 803: job_contract_version=(
FILE: backend\server\runtime\runtime_versioning.py
LINE 805: job_contract_version
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 33: UDARE_RUNTIME_CONTRACT_VERSION = (
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 227: UDARE_RUNTIME_CONTRACT_VERSION,
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 270: UDARE_RUNTIME_CONTRACT_VERSION,
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 609: "runtime_contract_version":
FILE: backend\server\runtime\udare_runtime_contract.py
LINE 610: UDARE_RUNTIME_CONTRACT_VERSION,
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 99: CERTIFICATION_CONTRACT_VERSION = "1.0.0"
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 321: certification_contract_version: str
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 437: "certification_contract_version": (
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 438: self.certification_contract_version
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 611: "contract_version": (
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 612: CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2202: CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2221: "certification_contract_version": (
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2222: CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2265: certification_contract_version=(
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2266: CERTIFICATION_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_schema\certification.py
LINE 2354: "CERTIFICATION_CONTRACT_VERSION",
FILE: backend\server\runtime\runtime_schema\registry.py
LINE 71: REGISTRY_CONTRACT_VERSION = "1.0.0"
FILE: backend\server\runtime\runtime_schema\registry.py
LINE 1839: "contract_version": (
FILE: backend\server\runtime\runtime_schema\registry.py
LINE 1840: REGISTRY_CONTRACT_VERSION
FILE: backend\server\runtime\runtime_schema\registry.py
LINE 2336: "REGISTRY_CONTRACT_VERSION",
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 40: UNIVERSAL_JOB_CONTRACT_VERSION: Final[str] = (
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 85: "contract_version",
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 491: contract_version: str = UNIVERSAL_JOB_CONTRACT_VERSION
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 509: "contract_version": self.contract_version,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 762: contract_version: str = UNIVERSAL_JOB_CONTRACT_VERSION
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 902: self.contract_version, name="contract_version", violations=violations,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 905: if version != UNIVERSAL_JOB_CONTRACT_VERSION:
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 907: "contract_version must be " + UNIVERSAL_JOB_CONTRACT_VERSION)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 908: set_(self, "contract_version", version or UNIVERSAL_JOB_CONTRACT_VERSION)
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 994: "contract_version": self.contract_version,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1022: "contract_version": (
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1023: UNIVERSAL_JOB_CONTRACT_VERSION
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1145: "contract_version": self.contract_version,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1157: "contract_version": (
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1158: self.contract_version
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1240: contract_version=candidate.contract_version,
FILE: backend\server\runtime\universal_jobs\contract.py
LINE 1293: "UNIVERSAL_JOB_CONTRACT_VERSION",
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 19: UNIVERSAL_JOB_CONTRACT_VERSION,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 493: "contract_version": UNIVERSAL_JOB_CONTRACT_VERSION,
FILE: backend\server\runtime\universal_jobs\creation_engine.py
LINE 812: "contract_version": UNIVERSAL_JOB_CONTRACT_VERSION,

============================================================
SCAN COMPLETE
NO FILES MODIFIED
NO JOB CREATED
NO QUEUE WRITE PERFORMED
NO WORKER EXECUTION PERFORMED
============================================================
